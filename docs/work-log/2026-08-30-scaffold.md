# 2026-08-30 — scaffold

**Task**: Scaffold the entire repo: Vite+React 19+TS+Phaser 3.90 corridor game, gallery
view, GitHub Pages deploy, hex-herbs-style agent conventions, PixelLab stubs.

**Spec §§**: GAME_DESIGN all; TECH_SPEC all (both authored this session, spec-first).

**Files changed**: everything (initial commit series). Toolchain, docs, 4 skills,
src/{bridge,data,game,components,hooks}, deploy workflow, .mcp.json + .env.example.

**Decisions**:
- Tiered fan-out used for authoring: haiku (meta files), sonnet ×4 (toolchain, docs,
  skills, src), opus ×4 adversarial review, sonnet ×3 fixes, opus verify. 40 findings
  (3 blockers) found and fixed; verify pass surfaced 7 more (2 high), fixed inline.
- Mode handshake: bridge records last `mode:changed`; scene reads `getCurrentMode()`
  in create() (lazy-load race fix — the blocker).
- Takeover = window-level keydown, filtered to game keys, no modifiers, ignores
  interactive targets; Phaser global capture disabled while a card is open.
- Whip hit test is directional (dx ∈ [-12, WHIP_RANGE+20] on facing side); autopilot
  is facing-aware with an 8px deadzone (anti-oscillation) + 45-frame retry cooldown.
- `audio: { noAudio: true }` until sound ships (kills AudioContext teardown errors).
- Placeholder art via generated textures; TEX keys are the art-swap contract.

**Deviations from spec**: initial commit series lands directly on `main` (bootstrap:
the branch must exist remotely before rule 7 can apply). All later work via feature/*.

**Tests/gates**: `pnpm gates` green — eslint clean, tsc clean, vitest 4 files /
28 tests. `pnpm build` clean (Phaser in lazy 1.2MB chunk; shell 202KB). Browser
playtest: autoplay carousel breaks lanterns 1-3 in order with correct cards,
auto-close + advance, husks persist, gallery + mobile-auto default verified,
console clean (bg-tab rAF throttling freezes Phaser — test with pane visible).

**Follow-ups**: PixelLab art pass (needs PIXELLAB_SECRET, ~$24 budget); gallery
card-to-dialog a11y (GD §9); sound; per-game deep links; verify .mcp.json header
env-expansion once a key is set.
